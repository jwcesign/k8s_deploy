apiVersion: v1
kind: Pod
metadata:
  name: scheduler
  labels:
    name: scheduler
spec:
  containers:
  - command:
    - sh
    - -c
    - "kube-scheduler --master=http://host_ip:8080 >> /var/log/scheduler.log 2>&1"
    name: kube-scheduler
    image: docker.io/kube-scheduler:1.10
    volumeMounts:
    - mountPath: /var/log/
      name: log
  volumes:
  - hostPath:
      path: /home/log/
      type: DirectoryOrCreate
    name: log
  hostNetwork: true